# Lefthook configuration
# See https://github.com/evilmartians/lefthook

pre-commit:
  parallel: false
  commands:
    build:
      run: uv run ./scripts/build.sh --with-coverage
      stage_fixed: true
